{{> header}}
<div class="container">
  {{#each posts}}
  <div class="post">
    <h2><a href="/post/{{id}}">{{title}}</a></h2>
    <p>{{content}}</p>
    <p>Posted by {{username}} on {{format_date createdAt}}</p>
  </div>
  {{/each}}
</div>
{{> footer}}